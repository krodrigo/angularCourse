import { ValidaCpfPipe } from './valida-cpf.pipe';

describe('ValidaCpfPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidaCpfPipe();
    expect(pipe).toBeTruthy();
  });
});
